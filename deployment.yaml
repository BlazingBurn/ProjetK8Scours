apiVersion: apps/v1
kind: Deployment
metadata:
  name: flask-deployment-dn
spec:
  replicas: 3
  selector:
    matchLabels:
      app: flaskdn
  template:
    metadata:
      labels:
        app: flaskdn
    spec:
      containers:
      - name: flaskdn
        image: torbthemagni/flask-actions-project
        # imagePullPolicy: "Always"
        ports:
        - containerPort: 5000
